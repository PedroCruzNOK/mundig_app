import 'package:flutter/material.dart';

class MunicipalityPage extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mi Municipio'),
      ),

    );
  }
}